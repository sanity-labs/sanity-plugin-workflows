---
"@sanity-labs/sanity-plugin-workflows": patch
---

Fix clean Studio startup by registering bundled fallback `user` and `lucide-icon` schema types for workflow audit entries and icons, resolving the schema compilation errors reported in [#9](https://github.com/sanity-labs/sanity-plugin-workflows/issues/9).
